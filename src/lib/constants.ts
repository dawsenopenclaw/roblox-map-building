/**
 * Platform-wide financial constants.
 * Update here to change the fee split everywhere it is used.
 */

/** Fraction of each sale retained by the platform (30%). Creator receives the remainder (70%). */
export const PLATFORM_FEE_PERCENT = 0.30
